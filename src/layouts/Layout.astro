---
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Astro description" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/images/link.png" />
		<link rel="stylesheet" href="/nes.min.css" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body class="bg-[var(--primary)]">
		<slot />
	</body>
</html>
<style is:global>
	@font-face {
		font-family: "pixel";
		src: url("/fonts/PressStart2P-Regular.ttf") format("truetype");
	}
	:root {
		--accent: 136, 58, 234;
		--accent-light: 224, 204, 250;
		--accent-dark: 49, 10, 101;
		--accent-gradient: linear-gradient(
			45deg,
			rgb(var(--accent)),
			rgb(var(--accent-light)) 30%,
			white 60%
		);
	}
	body {
		font-family: "pixel", sans-serif;
		color:white
	}
	.shadow-text {
		text-shadow: 5px 4px 0px #000000;
	}
	.text-border-lg {
		text-shadow:
			-5px -5px 0 #000,
			5px -5px 0 #000,
			-5px 5px 0 #000,
			5px 5px 0 #000;
	}
	.text-border-md {
		text-shadow:
			-2px -2px 0 #000,
			2px -2px 0 #000,
			-2px 2px 0 #000,
			2px 2px 0 #000;
	}
	.text-border-sm{
		text-shadow:
			-1.4px -1.4px 0 #000,
			1.4px -1.4px 0 #000,
			-1.4px 1.4px 0 #000,
			1.4px 1px 0 #000;
	}
	html {
		scroll-behavior: smooth;
	}
	a{
		text-decoration: none;
	}
	a:hover{
		text-decoration: none;
		color: currentColor;
	}
</style>
